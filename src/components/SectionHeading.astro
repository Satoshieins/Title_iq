---
const { eyebrow, title, intro } = Astro.props;
---
<div style="margin-bottom:40px">{eyebrow && <span class="eyebrow">{eyebrow}</span>}<h2 class="h2">{title}</h2><div class="rule"></div>{intro && <p class="lead" style="margin-top:22px">{intro}</p>}</div>
