---
interface Props { variant?: "colour" | "white" | "black"; decorative?: boolean; }
const { variant = "colour", decorative = false } = Astro.props;
---
<img src={`/assets/logo-${variant}.png`} width="390" height="140" alt={decorative ? "" : "TitleIQ & Consultancy"} />
